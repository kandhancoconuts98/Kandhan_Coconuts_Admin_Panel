# Supabase (server-side)
# In Supabase Dashboard → Project Settings → API
#
# NOTE: Use the Service Role key ONLY on the server (never in NEXT_PUBLIC_).
# The app will use a server API route to push/pull farm data.

NEXT_PUBLIC_SUPABASE_URL=https://tfrrqepscxcmkxhzhmwo.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_38RwPfcCVhKdEFImcxmVIg_U9fxMabu

# Server-only (Vercel env var) — never expose in NEXT_PUBLIC_
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Single-tenant farm ID (server-only). Pick any stable string.
COCONEST_FARM_ID=Kandhan Coconuts

# React Native app (mobile/.env) — same values, use EXPO_PUBLIC_API_URL
# EXPO_PUBLIC_API_URL=https://your-app.vercel.app
